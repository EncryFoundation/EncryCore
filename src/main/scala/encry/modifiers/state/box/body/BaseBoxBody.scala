package encry.modifiers.state.box.body

import scorex.core.serialization.JsonSerializable

// Parental trait for each `*BoxBody` class.
// Forms the structure of `Transaction`.
trait BaseBoxBody extends JsonSerializable
